//- Include Local Data
- const base = ""

//- Include Head
include ../includes/head

// Contenedor Principal
main.content
    //- Hero Section (usa el mixin; debe estar definido en includes/head o en un include anterior)
    +hero_section('assets/img/icons/mirror_ball.webp', 'La noche nunca termina', 'Tu acceso VIP al mejor underground electrónico')

    //- Sección de búsqueda principal: permite filtrar eventos
    section.main-search
        //- Campo de texto para escribir el nombre del evento
        input.search-input(type='text' placeholder='¿Qué evento buscas?')

        //- Menú desplegable para seleccionar el género musical
        select.search-select
            option Géneros
            option Techno
            option House
            option Trance
            option Drum &amp; Bass
            option Progressive

        //- Segundo campo de texto para escribir la ciudad del evento
        input.search-input(type='text' placeholder='Ciudad')

        //- Botón de acción para ejecutar la búsqueda
        button.search-button
            img(src= base + 'assets/img/icons/fire.webp' width='30' alt='telemoji fire' style='position:absolute;margin-left:-21px;margin-top:-6px')
            |  Buscar

        //- Descripción más extensa que da contexto al contenido
        p.subtitle Descubre las fiestas más exclusivas, DJs internacionales y la escena electrónica más vibrante de tu ciudad.

    //- Sección de estadísticas
    section.stats-section
        .stats-grid
            .stat-item
                .stat-number 2.5K+
                .stat-label Fiestas organizadas
            .stat-item
                .stat-number 150+
                .stat-label DJs internacionales
            .stat-item
                .stat-number 50K+
                .stat-label Ravers conectados
            .stat-item
                .stat-number 20+
                .stat-label Ciudades bailando

    //- Sección de géneros musicales
    section.genres-section
        h2.section-title Géneros que mueven la noche
        .genres-grid
            each value, index in events.category_events
                +card_event_category(value.name, value.description)

    //- 🏠 Grilla de Eventos
    section.featured-events
        // 🏷️ Título principal de la sección
        h2.section-title Próximos eventos destacados

        // 📦 Contenedor en formato grid con 3 columnas adaptables
        .events-grid
            // Todos los Eventos
            each value, index in events.inperson_events
                +card_event(
                    "#",
                    value.name,
                    value.date,
                    value.place,
                    `${value.start_time} - ${value.end_time}`,
                    value.category,
                    value.price_from
                )

//- Local Widget Components
//- Local Libs
//- Local Configs

//- Local JS Script
script.
    console.log("Hola Mundo desde 🎟️ TicketPass 😎")

//- Include Footer
include ../includes/footer



